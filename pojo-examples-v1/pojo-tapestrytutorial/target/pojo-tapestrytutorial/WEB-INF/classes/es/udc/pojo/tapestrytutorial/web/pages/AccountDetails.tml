<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:t="http://tapestry.apache.org/schema/tapestry_5_3.xsd" 
      xmlns:p="tapestry:parameter">

<head>
	<meta charset="utf-8" />
	<title>${message:title}</title>
</head>

<body>

<div class="container">

	<h3 class="text-center">${message:title}</h3>
	
	<br/>

	<t:if test="account">
	
		<table class="table table-striped table-hover">	
			<tbody>
		        <tr>
		            <th>${message:accountId-label}</th>
		            <td>${account.accountId}</td>
		        </tr>		
		        <tr>
		            <th>${message:userId-label}</th>
		            <td>${account.userId}</td>
		        </tr>		
		        <tr>
		            <th>${message:balance-label}</th>
		            <td>${account.balance}</td>
		        </tr> 	
	        </tbody>	
		</table>
	       	
		<p:else>
			<h4 class="alert alert-danger text-center" role="alert">${message:accountNotFound}</h4>		       		
		</p:else>
	
	</t:if>
	
	<p>
		<a href="#" t:type="PageLink" t:page="index">${message:link-home}</a>
	</p>

</div>

</body>

</html>
